class Solution {
    public ListNode middleNode(ListNode head) {
        ListNode two = head;
        ListNode one = head;
        while(two!=null && two.next!= null){
            one=one.next;
            two=two.next.next;
        }
        return one;
    }
}
